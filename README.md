django-stubtools
================

Manager app extensions to help "stub out" common Django features

Upload the file to PyPi: python setup.py sdist upload

python setup.py build ; python setup.py install -f

